# Redis only. Postgres runs natively from the PostgreSQL 18 install on :5432
# (see README "Getting started") — do not add a postgres service back here
# without changing its port, or the two will fight over 5432.

services:
  redis:
    image: redis:7
    container_name: dudu_redis
    ports:
      - "6379:6379"
